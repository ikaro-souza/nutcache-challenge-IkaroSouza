{
  "tasks": [
    {
      "label": "launch app",
      "type": "shell",
      "command": "yarn; yarn dev",
      "options": {
        "cwd": "${workspaceFolder}\\app"
      }
    }
  ],
  "version": "2.0.0"
}
